[project]
name = "receita"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Arthur", email = "antoniamartinsphilippcarneiro@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pymysql>=1.2.0",
    "sqlalchemy>=2.0.54",
]

[project.scripts]
receita = "receita:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
